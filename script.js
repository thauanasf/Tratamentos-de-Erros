function getAdmins(map) {
let admins= [];
    for([key, value] of map){
    if(value=="Adm")
}
}

const usuarios = new Map();

usuarios.set ("Luiz", "Admin");
usuarios.set ("Thauana", "Admin");
usuarios.set ("Jorge", "User");
usuarios.set ("Natália", "Admin");

console.log(getAdmins(usuarios));